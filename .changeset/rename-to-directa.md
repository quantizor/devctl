---
"directa": major
---

The product is now called directa. The CLI is `directa`, the daemon is `ddirecta`, the menu bar app is `directa.app`, and deep links use `directa://`. `devservers.json` is unchanged.
